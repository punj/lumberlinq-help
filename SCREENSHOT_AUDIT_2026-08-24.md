# KB Screenshot & Coverage Audit — 2026-08-24

Research-only audit. No screenshots taken, no markdown edited, nothing committed. Every row cites
its evidence (exact file + line, or "confirmed via reading X"). Items I could not verify directly
are marked **UNVERIFIED** rather than guessed.

---

## Stale/Renamed Terminology Found

| File | Line | Quoted text | Verdict | Suggested fix |
|---|---|---|---|---|
| `reports/user-manual.md` | 36, 50, 61, 129, 146 | "Total Shipments" (KPI card label, 5 occurrences across Business Partner/Product/Location/Consignment/Payment reports) | **Genuine stale** — confirmed via `tms-ng/src/app/i18n/translations.ts:1171`: the real i18n key `report.kpi.totalShipments` renders as **"Total Consignments"** in the live English UI, not "Total Shipments". The KB text doesn't match what a user actually sees on screen. | Replace every "Total Shipments" in `reports/user-manual.md` with "Total Consignments" |
| `reports/user-manual.md` | 148 | "shipments, total invoiced, fully paid..." (Payment Report "By Currency" tab column list) | **Likely same issue, not individually verified** — same report module, same rename pattern, but I did not confirm this specific column's i18n key. Flag for the same fix, verify the key before changing. | Verify `report.kpi`/column key, then align wording |
| `platform-basics/user-manual.md` | 89 | "Dashboard, Shipments, Tally Sheet, Reports, Business Partner..." (Product Tour page list) | Genuine stale (minor) — inconsistent with the rest of the KB's "Consignments" terminology; the app's own Product Tour step titles were not checked line-by-line but every other live UI label found this session says Consignment. | Change "Shipments" → "Consignments" |
| `subscriptions/user-manual.md` | 10 | "limits (tallies, shipments, users, storage...)" | Same minor inconsistency as above. | "shipments" → "consignments" |
| `account/faq.md` | 56 | "my tally sheets and shipments after I delete my account" | Same minor inconsistency. | "shipments" → "consignments" |
| `shipments/user-manual.md`, `shipments/faq.md` | whole files | Category/folder is named `shipments`, URL is `/shipments/user-manual/` | **False positive / structural note, not a text error.** Confirmed via `app-routing.module.ts:759-767`: `/shipments` is a real, live, permanently-kept **legacy alias route** for `/consignments` ("Legacy alias for bookmarked/emailed links using the old 'shipments' path" — deliberate, not dead code). All body text inside these two files already correctly says "Consignment" throughout (list page, tabs, forms) — only the category slug itself is the old name. Not urgent; renaming the folder is a bigger, separate decision (breaks the existing help.lumberlinq.com URL) and is out of scope for a text fix. | No action needed unless you want to rename the URL slug itself (separate decision) |
| `account/user-manual.md` | 109 | "Shipments \| Updates on your shipments and consignments" (notification preferences table) | **False positive.** Confirmed this is the literal backend `NotificationCategory.SHIPMENTS` enum value (see CLAUDE.md rule 12's own category list: `SHIPMENTS`), which really is the toggle's name in `AccountDetailsComponent`. Accurately describing the real system name. | No fix needed |
| business-partners, affiliate-program, tally-sheets, products (all "Buyer"/"Seller"/"Hoppus" mentions) | various | "Buyer, Seller, or Both" (Party Type field), "Round Log — uses Hoppus-style measurements" | **False positives.** "Buyer"/"Seller" here describe the live `Party Type` field on Business Partner (confirmed real field, not the deprecated standalone Buyer/Seller modules). "Hoppus" describes a measurement style used inside the active Round tally form, not the deprecated `hoppus`/`hoppus-measurement` module folders. | No fix needed |
| `products/user-manual.md:32` | 32 | "Square / Sawn — uses rectangular volume measurement for sawn timber" | **False positive.** Describes a live Product Type field value, unrelated to the deprecated `sawn-timber` module/route. | No fix needed |
| Everywhere else (Bill of Lading, Packing List, Promocode, TradeEvent, PackinglistCompany, Grove) | — | e.g. "BL, bill of lading, packing list" in synonym lines; "grove plan" in `subscriptions` synonym lines only | **False positives.** BL/packing list mentions are all about the real document fields/tabs on a live Consignment (confirmed against `shipments/user-manual.md`'s Documents tab section). "Grove plan" appears only inside "Also searched as" synonym lines (a deliberate old-name search alias, not asserted as the current plan name) — the article's real body text correctly names the current top tier "Forest" (`subscriptions/user-manual.md`, "## Forest Plan" section). Zero hits anywhere for "Promocode", "TradeEvent", or "PackinglistCompany" leaking into user-facing text. | No fix needed |
| Quick Search / Global Search / Ctrl+K outside `platform-basics/` | — | — | **Confirmed clean.** Grepped the entire docs tree; the only remaining hits are the already-fixed `platform-basics/faq.md:40-42` (the deliberate "why doesn't Ctrl+K work anymore" explainer Q&A, correct on purpose) and the already-fixed cross-references in `subscriptions/user-manual.md` from earlier this session. No other file mentions these anywhere. | No fix needed |

**Side note (not the main deliverable, flagged as instructed):** `CLAUDE.md`'s own "Old Dashboard" table entry says "Replaced by `dashboard-v4/`" — confirmed stale. Reading `app-routing.module.ts:809-827` directly: `dashboard-v3`, `dashboard-v5`, `dashboard-v6`, and `dashboard-new` all now redirect to `dashboard-v7` (retired 2026-08-01 per the code's own comment), and `dashboard-v7` is the only live route. The KB itself already correctly says `/dashboard-v7` in two places (`command-center/user-manual.md:12`, `platform-basics/user-manual.md:10`) — this is a `CLAUDE.md` staleness issue, not a KB issue.

---

## Routes With Zero KB Coverage

Read the full `app-routing.module.ts` (918 lines). Excluded: every route under `path: "admin"` (guarded `["ROLE_SUPER_ADMIN", "ROLE_ROOT", "ROLE_SUPPORT"]` at `app-routing.module.ts:876` — platform-operator tooling, out of scope per the no-admin-only-screenshots rule), all confirm-only/error/redirect routes, and all deprecated-module routes (`sawn-timber`, `measurement`, `hoppus*`, `container`, `bill-of-lading`, `buyer`, `seller` — per `CLAUDE.md`'s own deprecated table, routes already commented out or dead).

| Route | Component | Why it matters |
|---|---|---|
| `subscription-opted-features`, `subscription-history` (children of `/subscriptions`) | `SubscriptionOptedFeaturesComponent`, `TransactionHistoryComponent` | Two real, live, `ROLE_ADMIN`-reachable subscription sub-pages. `subscriptions/user-manual.md` mentions "Payment History" and downloading invoices generically but neither this exact route nor a "Subscription Features" view is named or shown. |
| `utility/unit-conversion`, `utility/volume-estimates`, `utility/slab-generator`, `utility/calculator` | `UnitConversionComponent`, `VolumeEstimateComponent`, `SlabGeneratorComponent`, `UtilityCalculatorComponent` | **Text-covered, zero screenshots.** `platform-basics/user-manual.md`'s "## Utility" section (lines 93-111) already documents all four tools in reasonable depth — but there is not a single screenshot anywhere in that section. Confirmed via grep: zero `screenshots/` references between lines 93-121. |
| `company/branding` | `CompanyBrandingComponent` | `ROLE_ADMIN`/`ROLE_REGISTRATION`-gated, real live route ("Manage your company logo"). No article or FAQ entry found anywhere in `company/` category mentioning company branding/logo upload. |
| `notifications` | `NotificationsPageComponent` | Any logged-in user's own notification history/inbox page (no role gate at all — `app-routing.module.ts:140-144`). Not mentioned anywhere in `account/` (which covers *preferences* for notifications, not the actual notification history page itself). |
| `locations` | `LocationSelectionComponent` | A generic address-picker route reachable app-wide. Low priority — likely embedded inside other flows rather than a standalone destination worth its own article. |
| `get-the-app` | `GetTheAppComponent` | Confirmed real, live, unguarded route. `SCREENSHOT_CHECKLIST.md`'s existing "Get the App" pending row already flags this correctly as awaiting confirmation of "the real current install method" — consistent with this finding, not a new gap. |
| `licenses` | `LicensesComponent` | Low-value legal/open-source licenses page — reasonable to skip. |

**Company-level Admin routes confirmed IN SCOPE (not excluded) and their KB status**, since the task asked me to distinguish company Admin from platform Admin:
- `consignment-field-access`, `tallysheet-fields-access` (`ROLE_ADMIN`) — **UNVERIFIED whether documented**; I did not find an obvious matching article/section during this pass and did not have budget to confirm either way. Flag for a follow-up check, don't treat as a confirmed gap.
- `settings/application` (`ROLE_ADMIN`, Application Settings incl. Payment Reminders) — documented, screenshot present (fixed this session).
- `settings/rbac` (`ROLE_ADMIN`) — documented in `rbac/` category (not re-verified for screenshot completeness this pass).
- `manage/user` (`ROLE_ADMIN`, User Management) — documented in `user-management/` category (not re-verified for screenshot completeness this pass).

---

## Dialogs Missing From KB

Excludes plain confirm-style dialogs (yes/no, no real explanatory content) and everything under `modules/admin/**` (SUPER_ADMIN/ROOT/SUPPORT-only). Evidence is a direct file+line read of each dialog's real header/content, not a guess from the component name.

| Dialog | Trigger location | Route/module | KB status |
|---|---|---|---|
| **Cancel Auto-Pay** | `subscription-opted.component.html:140-164` — "Cancel Auto-Pay" button on the Subscription page, typed-`CANCEL`-to-confirm dialog with 3 real consequence bullets (keep access until period end, no refund, won't auto-renew) | `/subscriptions` (`ROLE_ADMIN`) | **Undocumented.** Grepped all of `subscriptions/user-manual.md` + `faq.md` for "Auto-Pay" — zero hits. This is materially different from the existing "Cancelling a Subscription" section (that describes ending the whole plan; this is specifically stopping the auto-renewal mandate while keeping current access) — a real, distinct, user-confusing action worth its own paragraph + screenshot. |
| **Add / Edit Operator** | `operator-dashboard.component.html:204-207` — same form dialog for both add and edit, triggered from Inventory → Operators | `/inventory` (Operators tab, `ROLE_ADMIN`/permission-gated) | **Documented in text** (`operators/user-manual.md:36`: "click Add Operator... Editing reuses the same dialog") **but zero screenshot.** Confirmed via grep: `operators/user-manual.md` only has 3 screenshots total (list-header, stats, search-filter) — none for this dialog. |
| **Ask Linc / AI Help chat widget** (general in-app help chatbot) | `app.component.html` — confirmed globally mounted (`ai-chat-widget.component.ts`/`.html`), app-wide, not tied to one module | Global | **Effectively undocumented.** The word "linc"/"chatbot"/"AI Help" appears ONLY inside "Also searched as" synonym lines across 9 files — zero actual body-text paragraphs anywhere in `src/content/docs/` describe what this widget is, how to open it, or what it can answer. This is a separate, real, live feature from the "Ask AI" *inside* Unified Search's Stock mode (which *is* now documented) — the general-purpose help chatbot itself has no dedicated coverage at all. Matches this session's own plan file finding (`smooth-splashing-bengio.md`) that the KB has no article for this. |
| **Share Chat Transcript** | `ai-chat-widget.component.html:886` | Same widget as above | Undocumented (follows from the parent widget being undocumented) |
| **Access Level Preview** | `shipment-export-dialog.component.html:60`, inside the Consignment Export dialog | `/consignments` export flow | **Partially documented.** The parent Export dialog IS screenshotted (`shipments/user-manual.md:122`, `shipments-16-export-dialog.png`), and the general Access Visibility Panel concept is documented elsewhere — but this specific "Access Level Preview" sub-dialog/button inside the export flow has no dedicated mention or screenshot. Low priority — it's a preview of something already explained conceptually. |
| **Scan to share (QR)** | `affiliate-dashboard.component.html:168` | `/affiliate/dashboard` (`ROLE_AFFILIATE`) | Blocked on the same pre-existing constraint as the rest of the Affiliate Dashboard — needs an approved affiliate login, already tracked in `SCREENSHOT_CHECKLIST.md`. Not a new finding, just confirming this specific sub-dialog exists once that login is available. |
| **Delete Role** | `rbac-settings.component.html:330-353` | `/settings/rbac` | **Correctly excluded** — read the full dialog: it's a plain yes/no delete confirmation with one warning sentence, no distinct content beyond a standard confirm pattern. Matches the task's own exclusion rule; listed here only to show it was checked, not skipped by accident. |

**Not exhaustively checked (ran out of budget this pass, flagging rather than guessing):** `transport-unit.component.html`, `shipments.component.html`, `dispatch-wizard.component.html`, `custom-made-request-dialog.component.html`, `documents-tallysheet-new`, `site-documents.component.html`, `company-branding`, `avatar-upload`, `add-user-to-existing-company`, `user-registration`, `tally-unit-gallery`, `application-settings` sections (`inventory-policy-settings`, `currency-settings`, `tally-defaults-settings`), `reconciliation-tallysheet`, `login.component.html`, `payment.component.html` sub-dialogs, `subscription-opted` beyond the Cancel Auto-Pay dialog already found. These files do contain `p-dialog` usages (confirmed via the earlier full grep of 68 files) but I did not open and read each one's actual header/content this pass. Recommend a targeted follow-up pass on this list specifically, prioritizing `dispatch-wizard` and `custom-made-request-dialog` first (see Priority Recommendation below — both belong to the Inventory article, which has a separate, larger, already-confirmed gap).

`extras/company/company.component.ts` — confirmed still routed (`registration/company`, `registration/register-company`, both `ROLE_ADMIN`) but is the `@deprecated` component per `CLAUDE.md`'s Dual Company Entity table (broken `CompanyService.getCompanyByUserId()`/`save()`/`update()` calls). **Do not document this dialog** — it's dead/broken code still technically reachable, not a real gap.

---

## AI Import — Round vs Square

Read the real current code directly (not the stale memory claim, which this confirms is now **wrong/outdated**):

- **Square** (`tallysheet-square-list.component.html:376-381`): AI Import button (`*appHasPermission="'TALLY_AI_IMPORT'"`) is live, calling `openAiImportDialog()` → opens `AiTallyImportDialogComponent`. It IS disabled — but only when `flexiblePreviewEnabled` (the Flexible View sub-mode is active), not globally. A code comment at line 330-338 confirms this precisely: "Also disabled (not hidden) in Flexible View — bulk import has no per-row product/origin/quality mapping yet... Disabled rather than removed because it's a user-togglable mode (Standard/Flexible), not a permanent role restriction."
- **Round** (`tallysheet-list-round.component.html:326-330`): AI Import button, same permission code, same `AiTallyImportDialogComponent`, live and enabled (only disabled if offline or the tally is shipped-locked — no Flexible View concept exists for Round at all, so there's no equivalent restriction to have).

**Conclusion: AI Import currently works for BOTH Round and Square in Standard View.** The prior memory note ("Square Flexible View Import/AI-Import now disabled (not hidden)... Round has no equivalent concept") was accurate only about the narrow Flexible-View-specific disablement, but reads as if AI Import is broadly disabled for Square — it is not.

**KB status: already correct, no fix needed.** `tally-sheets/user-manual.md:166` already says: "AI Import supports image-based extraction for handwritten tally sheets (both Round and Square) and shows AI credit usage" — this matches the real code exactly. **However, zero screenshot exists for the AI Import dialog itself** — confirmed via grep, `tally-sheets/user-manual.md`'s "## Import and AI Import" section (lines 161-166) has no image reference at all, and neither does the regular (non-AI) Import dialog (configure/upload/map/preview steps, also described with zero screenshots).

---

## Broken Screenshot References

Checked all 184 unique `/screenshots/...` references across every article against `public/screenshots/` on disk (`grep` + file-existence check for each). **Zero broken references found.** The one pre-existing broken reference (Payment Reminders) was already fixed earlier this session.

---

## Priority Recommendation

Ranked by real user impact, using only confirmed findings above:

1. **Inventory article has literally zero screenshots** — confirmed via grep, not one `screenshots/inventory/` reference exists in `inventory/user-manual.md` despite it covering Overview, In-Out, Processing (Custom-Made/Re-saw runs), and pointing readers to a separate Operators guide. The only inventory-related screenshots in the whole KB live under `shipments/user-manual.md` (`shipments-27` through `shipments-33`), a historical artifact from when Inventory was a tab inside the Shipments module. This is the single biggest visual gap in the KB.
2. **Reports article's "Total Shipments" labels are factually wrong** — confirmed against the real i18n key (`report.kpi.totalShipments` → "Total Consignments" in the live UI), appearing 5 times across 4 different report sections. A reader following the KB text while looking at the real screen will not find what the article says to look for.
3. **The general "Ask Linc" AI Help chatbot has no article at all** — confirmed via exhaustive grep, only synonym-line mentions exist app-wide, zero real explanatory content, despite this session's own plan file (`smooth-splashing-bengio.md`) independently flagging the exact same gap from the chatbot's own knowledge-base side. High visibility (global widget, every page) + zero documentation is the widest reach/coverage mismatch found.
4. **Cancel Auto-Pay dialog is undocumented** — a real, distinct, easy-to-misunderstand billing action (stops renewal but keeps current access, no refund) with zero KB coverage; billing confusion support tickets are exactly what this dialog exists to prevent, and right now the KB can't help with them.
5. **Utility tools (`utility/*` — Calculator, Unit Conversion, Cost Estimate, Slab Generator) are text-documented with zero screenshots** — four real, live tools, already well-described, just need images.
6. **Add/Edit Operator dialog has no screenshot** — text already correct, quick win.
7. **Subscription Features / Transaction History sub-pages have no dedicated coverage** — two real live routes under `/subscriptions` with no specific mention.
8. **Company Branding page (`company/branding`) is undocumented** — real `ROLE_ADMIN` feature (logo upload), zero mentions found in `company/` category.
9. **AI Import / regular Import dialogs have no screenshot** — text is already accurate (rare and worth protecting), just needs the visual.
10. **`consignment-field-access` / `tallysheet-fields-access` KB status is unresolved** — flagged as UNVERIFIED above; worth a quick dedicated check before assuming either way, since both are real `ROLE_ADMIN` routes controlling field-level visibility, a fairly advanced/support-ticket-prone feature.
11. **Notifications history page (`/notifications`) is undocumented** — distinct from the already-documented notification *preferences* screen; every logged-in user can reach it.
12. **The "not exhaustively checked" dialog list above** (`dispatch-wizard`, `custom-made-request-dialog`, `transport-unit`, etc.) should get a dedicated follow-up pass — `dispatch-wizard` and `custom-made-request-dialog` specifically are very likely to compound finding #1 (Inventory's zero-screenshot gap), since both are core Inventory workflows.
13. **Minor terminology cleanup** — 4 leftover lowercase "shipments" mentions outside the `shipments/` category itself (`platform-basics`, `subscriptions`, `account/faq`) — low effort, low urgency, but easy to batch with any other pass through those files.
14. **`shipments/` category slug/URL still says "shipments"** — not a text bug (all body content is correct), but worth a conscious decision on whether to eventually rename the URL to `/consignments/user-manual/` with a redirect, matching the app's own `/consignments` primary route.
15. **`CLAUDE.md`'s deprecated-modules table has a stale entry** ("Old Dashboard... Replaced by `dashboard-v4/`") — should say `dashboard-v7`; a one-line fix in the project's own instructions file, not the KB, flagged here only because it was discovered during this pass.

---

*Audit method: direct reads of `app-routing.module.ts` (full file), `translations.ts` (targeted grep), all `p-dialog` occurrences app-wide (68 files located, ~15 read in full), every markdown file under `src/content/docs/` (full-text grep sweep + targeted reads), and a full-repo file-existence check for all 184 screenshot references. No finding above is based on a route/component name alone without reading its actual guard, template, or i18n value.*

---

## Follow-Up: Overlay/Dropdown/Popover Surfaces Missed By The First Pass

The first pass only grepped for routes and `p-dialog`. It missed an entire category of UI surface — overlay panels, popups, and drawers that aren't full-page routes or `p-dialog`s. This pass grepped `tms-ng/src/app/**` for `p-overlayPanel`/`p-popover`/`p-sidebar`/`p-drawer`/`p-menu`, read each real hit in full, and cross-checked KB coverage the same way as the first pass (deprecated-module folders like `container`, `bill-of-lading`, `buyer`, `seller`, `measurement`, `hoppus-measurement` excluded per `CLAUDE.md`).

| UI surface | Trigger / location | Evidence | KB status |
|---|---|---|---|
| **Notification Bell dropdown** | Bell icon in the header, every logged-in user, no role gate | `tms-ng/src/app/notifications/notification-bell/notification-bell.component.html` (full file read) — a `p-popover` containing: panel title, a "Mark all read" button, a scrollable list of notification cards (priority dot, title, body, timestamp in the recipient's own timezone), **Accept/Decline buttons on invite-type notifications while unread** (a real, easy-to-miss action), an Accepted/Declined resolution badge once read, and a "View all" footer link to the full `/notifications` history page | **Undocumented.** Grepped `src/content/docs/` for "bell"/"mark all read"/"accept...decline"/"notification panel" — the word "bell" appears twice (`account/faq.md:40`, `account/user-manual.md:97`) but only as a passing reference ("the bell always shows it") — the panel's actual content, and specifically the Accept/Decline invite action living here, is never described anywhere. This is a bigger gap than it looks: a reader who declines/accepts a team invite from this dropdown has no documentation of that action existing at all. |
| **Mobile Preferences drawer** | "Preferences" icon in the header, mobile/narrow viewport only | `tms-ng/src/app/modules/layout/header/header.component.html:232-341` — a `p-drawer` (title "Preferences", sliders icon) duplicating the desktop header's Language/Font Size/Theme/Dark Mode controls for small screens | **Same content already documented for desktop** (`platform-basics/user-manual.md` describes Language/Font/Theme via the header). Low priority — this is a responsive re-layout of an already-covered feature, not a separate undocumented feature. Worth a screenshot only if the KB ever adds mobile-specific screenshots generally; not worth a dedicated text section. |
| **AI Chat Widget's own panel + emoji popover** | Chat launcher button (global, every page) | `tms-ng/src/app/modules/layout/ai-chat-widget/ai-chat-widget.component.html:26` (`p-drawer`, left-side slide-out chat panel) and `:876` (`p-popover #emojiPanel`, emoji picker inside the compose box) | Confirms and details the ALREADY-reported first-pass finding ("Ask Linc has no article at all") — not a new separate gap, just concrete detail: the chatbot is a left-side drawer, not a bottom-corner popup, and it has its own emoji picker. No separate action needed beyond the article this first audit already called for. |
| **Account/profile menu (`#accountMenu`, `p-menu`)** | Avatar icon in the header, every user | `tms-ng/src/app/modules/layout/side-bar/side-bar.component.ts:397-427` — Edit Profile, Storage, Product Tour, **Switch Company** (if 2+ memberships), Leave Company, Logout | **Already documented, not a gap.** `account/user-manual.md:37-39` describes Switch Company with a real screenshot (`header__company-switcher__01.png`). "Leave Company" isn't separately called out but is a minor, self-explanatory item — not worth flagging as a real gap. |

**Not fully checked, flagged rather than guessed:** `p-menu`/`p-tieredMenu` usages elsewhere in the app (e.g. row-action kebab menus on list pages) were not individually opened this pass — those are generally per-row action menus already implied by each list page's own documentation (edit/delete/etc.), judged lower-risk of being a hidden undocumented feature than a global, always-present surface like the bell. A dedicated pass could still confirm this rather than assume it.

### Ranked by real impact

1. **Notification Bell dropdown, specifically the Accept/Decline invite action** — highest-impact new finding. It's global (every user sees it), and the Accept/Decline action is a real, consequential, one-click decision with zero documentation anywhere.
2. **Notification Bell's general content** (Mark all read, per-item cards, View all link) — worth documenting alongside #1 in the same KB pass, likely belongs in `account/user-manual.md` near the existing Notification Preferences section, or as its own short subsection.
3. Mobile Preferences drawer and the AI Chat Widget's drawer shape are lower priority — both are either already covered in substance (desktop preferences) or already flagged for a full article (Ask Linc) in the first pass.

---

## Full Re-Audit (Global Chrome + All Overlay Patterns) — 2026-08-24, Round 2

Supersedes the narrower grep-list approach above where they overlap; does not repeat any finding already confirmed in the sections above. This pass read `app.component.html` (412 lines) and `app.component.ts` (1113 lines) in full — every persistent, app-wide UI surface mounted at the root, regardless of route — plus `header.component.html` in full, then a broader overlay-pattern grep (`p-tieredMenu`, `p-speedDial`, `p-contextMenu`, `p-slideMenu`, `p-megaMenu`, `p-splitButton`) across all of `tms-ng/src/app/**/*.html`.

**Notification Bell — independently re-verified, confirms Round 1 exactly.** Read `notification-bell.component.html` in full (89 lines): bell icon with unread-count badge → `p-popover` panel showing a header ("Mark all read" button when unread items exist), a scrollable list of notification cards (priority dot, title, body, timestamp rendered in the *recipient's own* timezone, not the viewer's device), **Accept/Decline buttons specifically on `ACCEPT_DECLINE_INVITE`-type items while still unread**, a resolution badge (Accepted/Declined, with a "switch to that company" hint) once read, and a "View all" footer link to the `/notifications` page. KB status independently re-checked via grep across all of `src/content/docs/` for "bell"/"mark all read"/"accept...decline"/"notification panel": only 3 hits, all in `account/user-manual.md:97`, `account/faq.md:40-42`, and `account/user-manual.md:106` — every one is a passing mention about push/browser buzz settings or being told someone else accepted/declined YOUR sent invite. **None of them describe the bell panel's own content, and the Accept/Decline action available when YOU receive an invite is not documented anywhere.** Confirmed genuine, high-impact gap.

**Dashboard route exclusion — checked, no correction needed.** The existing "Routes With Zero KB Coverage" table above does not list `dashboard-v3`/`dashboard-v5`/`dashboard-v6`/`dashboard-new` as gaps — nothing to fix. `dashboard-v7` is confirmed the only real, user-facing dashboard route (per the file's own earlier side-note); the other four are dead redirects.

| New UI surface | Trigger / location | Evidence | KB status |
|---|---|---|---|
| **SpeedDial FAB** (bottom-right floating button) | Every page with `showMenu` true and online; `app.component.html:217-221` | `app.component.ts:657-676` (`buildSpeedDialItems()`) — exactly 2 items: **"Linc AI Help"** (opens the AI chat widget) and **"Support Tickets"** (badge = open ticket count, navigates to `/support/tickets`) | **This is the actual, real entry point for opening the Ask Linc chatbot on desktop** — not a header icon. Compounds the already-known "Ask Linc has zero KB article" finding: a reader has no way to even learn this button exists. Support Tickets itself is already documented (`platform-basics/user-manual.md` "## Support Tickets"), but never via this specific button — minor addition once Ask Linc's article gets written. |
| **Legal Update Banner** | Any page, any logged-in user, only when a legal doc (ToS/Privacy/Cookie policy) has a newer version than the user last accepted | `app.component.html:196-210`, driven by `handleOutdatedLegalDocs()` (`app.component.ts:957-964`) — dismissible banner naming the updated doc(s) with links to `/legal/:type` and to Company Profile's Legal tab | **Undocumented.** Zero mentions found anywhere in `src/content/docs/` for "legal update"/"documents updated"/this banner's behavior. Real, if infrequent — worth one short FAQ entry (likely `account/faq.md` or `company/faq.md`) rather than a full section, since it's rare and self-explanatory once seen. |
| **Forced Logout dialog** | Any page — fires when this session's own request gets 401'd and traces to a same-account "log out that device" action taken elsewhere | `app.component.html:265-292`, `app.component.ts:353-368` — names the device type/name and location that triggered the kick, distinct wording from the generic Session Expired dialog | **Undocumented**, and ties directly to `NotificationType.SESSION_FORCED_LOGOUT` (already a known real notification type per `CLAUDE.md` rule 12's category list) and the existing `ConfirmLogoutDevicesComponent` "log out that device" feature — but nothing in the KB explains what a user sees ON THE OTHER END when they get force-logged-out. Real, security-relevant, moderate priority — a confused user hitting this with no explanation is exactly the support-ticket risk pattern already flagged for Cancel Auto-Pay in Round 1. |
| **Cookie Consent banner + Cookie Preferences dialog** | First-time visitor (any page, logged in or not) | `app.component.html:345-400`, `app.component.ts:977-1005` — Accept All / Reject Non-Essential / Manage buttons; Manage opens a dialog with 3 toggles (Essential — locked on, Functional — locked on, Analytics — user-controlled), geo-aware default (GDPR/UK-GDPR/DPDP regions default to opted-out) | **Undocumented**, but low priority — standard compliance boilerplate, not a product feature most readers would search the KB for. Worth a single FAQ line at most ("Why do I see a cookie banner?"), not a dedicated section. |
| **Get the App banner** (in-page dismissible bar, distinct from the `/get-the-app` route already noted in Round 1) | Mobile browser (Android/iOS user-agent) visiting the web app, not already in the native app | `app.component.html:85-90`, `app.component.ts:1035-1072` — platform-aware CTA, 30-day re-show window after dismissal | Same underlying gap as the already-tracked `/get-the-app` route finding — this is a second, separate surface (an app-wide dismissible banner, not the dedicated page) advertising the same thing. Worth a one-line mention alongside whatever the existing tracked item resolves to, not a new separate priority item. |
| **Offline prefetch bar + sync checklist** | Any page, only while `OfflinePrefetchService.isRunning()` | `app.component.html:140-156` — a progress-fill bar plus a live checklist of sync areas, each with a status icon (done/running/error/pending) | Falls entirely under the already-known, already-tracked "Offline Mode & Mobile App" gap (4 pending items in `SCREENSHOT_CHECKLIST.md`) — this is additional detail on what that category should cover, not a new separate finding. |
| **Plan Limit dialog** (modal, distinct from the already-documented Plan Limit *Banner*) | Any page, fires on a 402/403 `PLAN_FEATURE_DISABLED` response — typically after a user has already filled out a create form and hits submit | `app.component.html:306-329`, `app.component.ts:376-385` — shows exact usage (`{used}/{limit}`), an "Upgrade" button, and a "Manage" button when a specific manage-route exists | **Minor gap.** `platform-basics/user-manual.md` documents the general "Plan Limit Banner" concept but not this specific post-submit modal variant. Low priority — same underlying concept, just a different trigger moment; a single sentence addition would close it, not worth its own screenshot. |

**Overlay-pattern grep results — no new findings beyond the above.** `p-tieredMenu`/`p-splitButton`/etc. matches outside `app.component.html` were either (a) inside already-deprecated modules per `CLAUDE.md`'s table (`seller`, `sawn-timber`, `measurement`, `hoppus`/`hoppus-measurement`, `container`, `bill-of-lading`, `buyer` — excluded, dead code) or (b) confirmed, on inspection, to be part of ALREADY-documented features: `view-transport-unit.component.html:77`'s `p-tieredMenu` and `:510`'s `p-splitButton` both belong to the existing Share Link System (Public/Protected/Private tiers — already fully documented per `CLAUDE.md`'s own Share Link System section), and `media-capture-button.component.html`'s `p-splitButton` (Take Photo/Record Video/Choose from Gallery) is a shared, mobile-only implementation detail of photo upload, already implicit wherever photo upload itself is documented — not a distinct feature worth its own section. No `p-contextMenu`, `p-slideMenu`, or `p-megaMenu` usage found anywhere in the app.

**Not covered this round, stated rather than silently skipped:** per-page tab/accordion/stepper components (`p-tabView`, `p-accordion`, `p-stepper`, `p-galleria`, per-row kebab `p-menu`s on list pages) were not individually re-opened this pass — these are lower-risk of hiding a genuinely undocumented feature since they live inside pages that already have their own KB article covering that page's main content, unlike the global, always-present surfaces this round focused on. A dedicated pass through list-page row-action menus specifically could still be worthwhile as a future, narrower follow-up.

### Ranked by real impact (Round 2 additions only)

1. **SpeedDial FAB is the real "Ask Linc" launcher on desktop** — folds directly into the already-#3-ranked "Ask Linc has no article" finding from Round 1; the article, when written, must mention this specific button, not just describe the chat panel in the abstract.
2. **Forced Logout dialog** — security-relevant, real, ties to an already-known notification type, zero documentation of what the user actually sees.
3. **Legal Update Banner** — small but real gap, one FAQ entry would close it.
4. **Plan Limit modal (vs. banner)** — minor text addition to the existing Plan Limits section.
5. **Cookie Consent / Get the App banner** — lowest priority, boilerplate/duplicate-of-tracked-item respectively.

---

## Two Confirmed Findings From Live Conversation (Not Yet In This File)

Surfaced directly by the user mid-conversation ("Shared link were also missing... check again"), then verified here against real code and the KB, not taken on faith.

| UI surface | Trigger / location | Evidence | KB status |
|---|---|---|---|
| **Share Link — Manage Audience dialog** | "Manage Audience" action on a Protected/Private share link | `tms-ng/src/app/modules/commons/share-link-audience-dialog/share-link-audience-dialog.component.ts` (full file read, 199 lines) — a `p-dialog` that lists everyone currently allowed to open the link (specific emails OR specific companies, `SPECIFIC_EMAILS`/`SPECIFIC_COMPANIES` audience type), lets you add a new entry (with duplicate detection) or remove one. Confirmed mounted and reachable from two real host pages: `shipment-list.component.html:848` (Consignment share flow) and `view-transport-unit.component.html:637` (Transport Unit share flow) | **Undocumented — confirmed via grep.** Searched all of `src/content/docs/` for "audience", "manage audience", "specific email", "specific compan": zero hits anywhere. |
| **Share Link — Access Audit panel** | Collapsible "Access Log"-style panel near a share link's management UI | `tms-ng/src/app/modules/commons/share-link-audit/share-link-audit.component.ts` (full file read, 116 lines) — shows every access attempt: IP (IPv4-normalized), browser, OS (parsed from user-agent), success/denied, bot-detection, timestamp, plus rollup counts (total accesses, success, denied, bot, unique visitors). Confirmed mounted and reachable from two real host pages: `shipments.component.html:1668` (Consignment detail view) and `dashboard-tallysheet-new.component.html:195` (Tally dashboard) | **Undocumented — confirmed via grep.** Same search as above plus "access log", "who accessed", "link audit": zero hits anywhere. |

Both are real, live, widely-mounted features (not a one-off or dead component) tied to the app's Share Link System, which otherwise has substantial existing KB coverage per `CLAUDE.md`'s own dedicated section — these two are a genuine, non-trivial gap in an area the KB mostly gets right.

---

## Tabs, Accordions, Steppers, and Row-Action Menus — Final Category

Last unchecked category, per the user's explicit "you must not miss anything" instruction after every prior pass deferred it. Grepped `tms-ng/src/app/**/*.html` for `p-tabView`/`p-tabs`/`p-accordion`/`p-stepper`, found ~30 host files, filtered out deprecated modules (`container`, `sawn-timber`, `measurement`, `hoppus*`, `bill-of-lading`, `buyer`, `seller`), admin-only, and stale `copy`/`_old_copy` backup files per `CLAUDE.md`. Checked the highest-value real pages against the KB; the rest are listed as explicitly not checked, not silently assumed clean.

| Surface | Host page | Evidence | KB status |
|---|---|---|---|
| **Dispatch Wizard steps** | `/inventory` → Send Out (`dispatch-wizard.component.html`) | `dispatch-wizard.component.ts:52-55` — 3 real steps: "Which stock is going out?" → **"Confirm stock split"** → "Where is it going?" (i18n keys resolved via `translations.ts:2114-2128`) | **Real gap.** `inventory/user-manual.md:44` only says "Send Out — dispatch stock (requires the Inventory Dispatch permission)" — one line, no mention this opens a 3-step wizard, and specifically no mention of the "Confirm stock split" step, which implies a real, non-obvious capability (splitting one stock lot's dispatch across multiple destinations/partial amounts). This compounds the already-known Inventory zero-screenshot gap (Priority #1 in the first pass) with a concrete missing capability, not just missing images. |
| **Receive Wizard steps** | `/inventory` → Stock In (`receive-wizard.component.html`) | `receive-wizard.component.ts:39-41` — 2 real steps: "What arrived?" → "How much?" | Same shape of gap — `inventory/user-manual.md:42` says "Stock In — receive new stock (requires the Inventory Receive permission)," one line, no mention of the 2-step wizard flow. Lower-impact than Dispatch's since neither step name implies a hidden capability the way "Confirm stock split" does — this is more a "no screenshot/no step-by-step" gap than a "missing feature" gap. |
| **Consignment tabs** (Details / Consignment Info / Stock Units / Documents / Financials & Payments / Dispatch & Notes) | `shipments.component.html:157-199` | Read all 6 real tab labels directly from the template | **Not a gap — already fully documented.** Cross-checked against `shipments/user-manual.md:47-51`: all 6 tab names are listed and described near-verbatim, each with its own subsection further down the article. |
| **Subscription purchase accordion** (Select Package → Currency → Billing → Payment) | `subscription-management.component.html:15-18+` (`p-accordion`) | Read the template header | **Not a gap — already fully documented with screenshots**, confirmed against `subscriptions/user-manual.md`'s "Upgrading Your Plan"/"Checkout and Payment" sections (Package selection, Currency dropdown, Yearly tenure, Order summary, Terms dialog — all already screenshotted). |
| **Row-action kebab menus** | Checked `shipment-list.component.html` (Consignments list) specifically | `shipment-list.component.html:95` — the only `p-menu` in the file is **commented out**, dead code | **Not a gap, and likely not a real risk category for this app.** This app's established list-page pattern (per `CLAUDE.md`'s own UI convention notes) is hover-reveal inline action buttons, not a kebab dropdown menu — so there's generally no *hidden* menu of extra actions on a list row waiting to be missed. This one page confirms the pattern; it was not re-verified on every other list page. |

**Explicitly NOT checked this pass** (stated, not silently assumed clean): `business-partner.component.html`, `company-details.component.html`, `tallysheet-list-round.component.html`, `tallysheet-square-list.component.html`, `view-tallysheet-shipment.component.html`, `tallysheet-view.component.html`, `inventory-report.component.html`, `share-shipment.component.html`, `slab-generator.component.html`, `tally-sheet-four.component.html`, and row-action menus on every list page other than Consignments (Business Partners, Products, Loading Sites, Users, Operators, Machines, Inventory lots, Tally lists, Transport Units) — the Consignments-list finding above is a reasonable signal the app-wide pattern avoids hidden kebab menus, but it is a signal from one page, not proof for all of them.

### Ranked by real impact (this section only)

1. **Dispatch Wizard's "Confirm stock split" step** — a real, named, non-obvious capability with zero documentation, inside the already-known biggest gap in the KB (Inventory). Should be folded into whatever content closes the Inventory zero-screenshot gap, not treated as a separate task.
2. **Receive Wizard's 2-step flow** — same fold-in, lower urgency since neither step name hides a distinct capability.
3. Everything else checked this pass (Consignment tabs, Subscription accordion, one list-page row-menu) came back clean — confirming the earlier passes' coverage was accurate for these specific pages, not a new gap.
